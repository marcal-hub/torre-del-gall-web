[project]
name = "latorredelgall-backend"
version = "1.0.0"
description = "La Torre del Gall website backend (FastAPI)"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "motor",
    "pymongo",
    "pydantic[email]",
    "python-dotenv",
    "httpx>=0.27.0",
]
